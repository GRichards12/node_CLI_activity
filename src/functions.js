const argv = require("yargs");

const addMovie = () => {
    for(let i=2; i<argv.length;i++){
    if(input[2] === "add"){
        const movieObj = new Movie(input[i.title],input[i.actor]);
        movieObj.add();
        const movieObj2 = new Movie(input[5],input[6]);
        movieObj2.add();
        console.log(movieObj1);
        console.log(movieObj2);
}}}